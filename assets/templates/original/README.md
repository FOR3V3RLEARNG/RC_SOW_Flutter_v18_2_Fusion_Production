# RC SOW operational templates

Place approved editable `.xlsx` and `.docx` templates here. The GitHub Production Factory packages them separately from the APK/AAB so management can retain editable originals.

The raw Shelter Roof Repair Assessment workbook is intentionally not packaged in release artifacts because it can contain beneficiary personal information. Beneficiary data should be imported into the protected backend/directory instead.
